'use client';

import React from 'react';
import { AddressAddModal } from './AddressAddModal';

export function AddressAddDialog(props) {
  return <AddressAddModal {...props} />;
}

export default AddressAddDialog;
